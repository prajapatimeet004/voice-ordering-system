services:
  voice-ordering:
    build: .
    container_name: voice-ordering-system
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./temp_audio:/app/temp_audio
      - ./logs:/app/logs
    restart: unless-stopped
